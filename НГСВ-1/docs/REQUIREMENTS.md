# Анализ требований для веб-приложения "Цифровой двойник трёхфазного сепаратора"

## 1. Разделение требований

### 1.1 Визуальные требования (Frontend/UI)

#### Структура интерфейса
- **Левая панель "Управление"**
  - Заголовок "Управление клапанами"
  - Три вкладки (табы):
    - "Регулятор давления"
    - "Регулятор уровня воды в основной камере"
    - "Регулятор уровня нефти в основной камере"

- **Центральная часть "Цифровой двойник"**
  - Заголовок "Цифровой двойник"
  - 2D модель трёхфазного сепаратора
  - Визуализация трёх фаз (газ, нефть, вода)
  - Трубопроводы с анимацией течения
  - Клапаны (интерактивные)
  - Насос (интерактивный)
  - Манометр давления
  - Панель с показателями (уровни газа, нефти, воды, расход)

- **Правая панель "Рекомендации"**
  - Динамические рекомендации на основе параметров системы
  - Цветовое кодирование статуса (норма, предупреждение, авария)

#### Визуальные элементы сепаратора
- **Форма**: прямоугольная с округлыми углами
- **Фазы**:
  - **Газ** (верх): серый с пузырьками, анимация движения
  - **Нефть** (середина): чёрного цвета
  - **Вода** (низ): голубого цвета, ограничена перегородкой
- **Граничные условия**: жидкости не выходят за границы сепаратора

#### Трубопроводы и их визуализация
- **Входная труба**: эмульсия (слева от сепаратора)
  - Анимация потока при работе насоса
- **Водяная труба**: голубая, с клапаном
  - Анимация течения воды при открытом клапане
  - Остановка при закрытом клапане
- **Нефтяная труба**: чёрная, с клапаном
  - Анимация течения нефти при открытом клапане
  - Остановка при закрытом клапане
- **Газовая труба**: с клапаном
  - Анимация течения газа с пузырьками при открытом клапане
  - Остановка при закрытом клапане

#### Интерактивные элементы
- **Клапаны**: кликабельные, изменяют визуальное состояние (открыт/закрыт)
- **Насос**: кликабельный, увеличивает расход жидкости
- **Ползунки (слайдеры)**: для регулировки параметров
- **Табы**: переключение между разными регуляторами

#### Цветовая схема
- Основные цвета: светло-голубой и белый
- Темный фон для контраста
- Голубая подсветка активных элементов

#### Алерты и уведомления
- **Красное мигающее окно**: для аварийных ситуаций
- **Временное отображение**: окна исчезают при нормализации параметров
- **Анимация мигания**: привлечение внимания

---

### 1.2 Функциональные требования (Backend/Business Logic)

#### Модуль 1: Регулятор давления
**Входные параметры**:
- `K` (waterK) - степень открытия водяного клапана (0-1)
  - 0 = закрыт
  - 0.5 = оптимально (открыт наполовину)
  - 1 = полностью открыт
- `Q` (inletFlow) - расход эмульсии (100-8000 м³/сут)

**Расчётная формула**:
```
P = 20.0 - 19.9 * sqrt( (K - 0.1) / (0.9 * (1 + Q/8000)) )
```
  где P - давление в МПа, K — степень открытия водяного клапана (waterK), Q — расход эмульсии (inletFlow, м³/сут)

**Логика алертов**:
- P > 10 МПа → АВАРИЯ: "Уменьшите напор насоса"
- P < 1 МПа → АВАРИЯ: "Увеличьте напор насоса"
- 1 ≤ P ≤ 10 → НОРМА (в МПа)

**Связь с насосом**:
- Клик на насос увеличивает расход Q
- Значение Q синхронизировано между слайдером и насосом

---

#### Модуль 2: Регулятор уровня воды
**Входные параметры**:
- `Cv` - коэффициент пропускной способности клапана (0-56 м³/ч)
- `ΔP` - перепад давления на клапане (0-8.5 бар)
- `u` - степень открытия клапана (0-1)
  - 0 = закрыт
  - 0.5 = оптимально
  - 1 = полностью открыт

**Расчётная формула обводнённости**:
```
Q_water = Cv × u × √ΔP
```
результат в процентах

**Логика алертов**:
- Q_water > 70% → АВАРИЯ: "Откройте клапан и сбросьте воду"
- Q_water < 30% → АВАРИЯ: "Прикройте клапан"
- 30% ≤ Q_water ≤ 70% → НОРМА

**Связь с клапаном**:
- Клик на клапан изменяет его состояние (открыт/закрыт)
- Состояние клапана синхронизировано со слайдером `u`

---

#### Модуль 3: Регулятор уровня нефти
**Входные параметры**:
- `Cv` - коэффициент пропускной способности (0-56 м³/ч)
- `ΔP` - перепад давления (0-8.5 бар)
- `u` - степень открытия клапана (0-1)

**Расчётная формула**:
```
Q_oil = Cv × u × √ΔP
```
результат в процентах

**Логика алертов**:
- Q_oil > 15% → АВАРИЯ: "Откройте клапан нефти"
- Q_oil < 0.5% → АВАРИЯ: "Закройте клапан нефти"
- 0.5% ≤ Q_oil ≤ 15% → НОРМА

**Связь с клапаном**:
- Состояние синхронизировано со слайдером

---

#### Модуль 4: Симуляция процесса разделения
**Входной поток**:
- Эмульсия разделяется на три компонента:
  - Вода: ~35% от входящего потока
  - Нефть: ~40% от входящего потока
  - Газ: ~25% от входящего потока

**Динамика уровней**:
- При открытом клапане: уровень фазы уменьшается
- При закрытом клапане: уровень фазы растёт
- Контроль переполнения: сумма уровней не превышает 100%

**Алерты по уровням**:
- Вода > 65% при закрытом клапане → АВАРИЯ
- Нефть > 55% при закрытом клапане → АВАРИЯ
- Газ > 60% при закрытом клапане → АВАРИЯ

---

#### Модуль 5: Система рекомендаций
**Классификация параметров**:

**Давление (P)**:
- P < 6 МПа → низкое
- 6 ≤ P ≤ 16 МПа → нормальное
- P > 16 МПа → высокое

**Уровень воды (W)**:
- W < 30% → низкий
- 30% ≤ W ≤ 70% → нормальный
- W > 70% → высокий

**Уровень нефти (H)**:
- 20% ≤ H ≤ 30% → низкий
- 40% ≤ H ≤ 55% → нормальный
- 60% ≤ H ≤ 70% → высокий

**Матрица рекомендаций**: 27 комбинаций (3×3×3)

Примеры ключевых режимов:
- **Нормальный**: P=6-16, W=30-70%, H=40-55% → "Поддерживать текущие установки"
- **Критический низкий**: P<6, W>70%, H>60% → "Экстренно снизить уровни, повысить давление"
- **Критический высокий**: P>16, W>70%, H>60% → "Немедленно снизить давление и уровни"

---

## 2. Архитектура решения (MVC)

### 2.1 Model (Модель) - `js/model.js`
**Ответственность**: Бизнес-логика, расчёты, состояние системы

**Функции**:
- Хранение состояния всех параметров
- Расчёт давления `calculatePressure()`
- Расчёт обводнённости `calculateWaterContent()`
- Расчёт уровня нефти `calculateOilLevel()`
- Классификация параметров для рекомендаций
- Генерация рекомендаций `getRecommendations()`
- Проверка аварийных состояний `checkAlerts()`
- Симуляция процесса `simulate()`
- Управление состоянием клапанов `toggleValve()`

**Преимущества**:
- Вся бизнес-логика в одном месте
- Легко тестировать независимо от UI
- Можно переиспользовать для других интерфейсов

---

### 2.2 View (Представление) - `js/view.js`
**Ответственность**: Отображение данных, манипуляция DOM

**Функции**:
- Получение ссылок на DOM элементы `getElements()`
- Обновление значений в контролах
- Обновление визуализации давления
- Обновление визуализации уровней в сепараторе
- Обновление состояния клапанов (визуально)
- Анимация потоков в трубах
- Показ/скрытие алертов
- Рендеринг рекомендаций
- Настройка табов

**Преимущества**:
- Изолирует работу с DOM
- Можно изменить дизайн без изменения логики
- Переиспользуемые функции отображения

---

### 2.3 Controller (Контроллер) - `js/controller.js`
**Ответственность**: Связывание модели и представления, обработка событий

**Функции**:
- Инициализация приложения `init()`
- Привязка обработчиков контролов `bindControls()`
- Синхронизация состояния клапанов и слайдеров
- Обработка кликов на клапаны
- Обработка клика на насос
- Координация обновлений UI при изменении модели
- Запуск симуляции
- Проверка алертов и обновление рекомендаций

**Преимущества**:
- Единая точка управления потоком данных
- Легко добавлять новые взаимодействия
- Разделение ответственности между моделью и представлением

---

### 2.4 Styles (Стили) - `css/styles.css`
**Ответственность**: Визуальное оформление

**Содержит**:
- Цветовую палитру (CSS переменные)
- Layout (Grid)
- Стили компонентов (кнопки, слайдеры, табы)
- Анимации (течение жидкости, мигание, пузырьки)
- Адаптивные стили
- Стили для алертов и рекомендаций

---

## 3. Соответствие реализации требованиям

### ✅ Реализовано
1. Трёхпанельный интерфейс (управление, двойник, рекомендации)
2. Табы для переключения между регуляторами
3. Все необходимые слайдеры и контролы
4. Визуализация сепаратора с тремя фазами
5. Трубопроводы с анимацией потока
6. Кликабельные клапаны с синхронизацией
7. Насос с увеличением расхода
8. Расчёт давления по формуле
9. Расчёт обводнённости и уровня нефти
10. Система алертов
11. Динамическая симуляция разделения фаз
12. Полная матрица рекомендаций (27 комбинаций)
13. Модульная архитектура (MVC)

### ⚠️ Отличия от исходной реализации
1. **Добавлена панель рекомендаций** (не было в старой версии)
2. **Улучшена синхронизация** клапанов и слайдеров
3. **Расширена система алертов** с учётом всех требований
4. **Улучшена формула** расчёта параметров
5. **Трёхпанельный layout** вместо двухпанельного

### 🔄 Улучшения
1. Разделение на модули для лучшей поддержки
2. Система подписок (Observer pattern) в модели
3. Централизованная обработка событий в контроллере
4. Улучшенная типизация параметров
5. Комментарии и документация в коде

---

## 4. Использование

### Запуск приложения
Откройте файл `index-new.html` в браузере (или настройте локальный сервер).

### Структура файлов
```
/
├── index-new.html          # Главный HTML файл
├── css/
│   └── styles.css          # Стили
├── js/
│   ├── model.js            # Бизнес-логика
│   ├── view.js             # Представление
│   └── controller.js       # Контроллер
└── REQUIREMENTS.md         # Этот документ
```

### Расширение функциональности
1. **Добавление нового параметра**: обновите Model
2. **Изменение UI**: обновите View
3. **Новое взаимодействие**: обновите Controller
4. **Изменение стилей**: обновите CSS

---

## 5. Технические детали

### Формулы
- **Давление**: `P = 20.0 − 19.9 × sqrt((K − 0.1) / (0.9 × (1 + Q/8000)))` (в МПа)
- **Обводнённость**: `Q_water = Cv × u × √ΔP`
- **Уровень нефти**: `Q_oil = Cv × u × √ΔP`

### Диапазоны параметров
- Kvs: 0-1
- Q (расход): 100-8000 м³/сут
- Cv: 0-56 м³/ч
- ΔP: 0-8.5 бар
- u (открытие): 0-1

### Пороги аварий
 - Давление: < 1 или > 10 МПа
- Обводнённость: < 30% или > 70%
- Уровень нефти: < 0.5% или > 15%
- Уровень воды в сепараторе: > 65% при закрытом клапане
- Уровень нефти в сепараторе: > 55% при закрытом клапане
- Уровень газа в сепараторе: > 60% при закрытом клапане

---

## 6. Преимущества модульной архитектуры

1. **Разделение ответственности**: Frontend отвечает за визуал, Backend за логику
2. **Тестируемость**: Можно тестировать модель независимо от UI
3. **Масштабируемость**: Легко добавлять новые функции
4. **Поддерживаемость**: Изменения локализованы в соответствующих модулях
5. **Переиспользование**: Модель можно использовать с другими интерфейсами
6. **Читаемость**: Код структурирован и хорошо документирован
