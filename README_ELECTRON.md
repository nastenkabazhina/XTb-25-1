# 🚀 Цифровой двойник МУПН - Desktop приложение

Десктопное приложение на базе Electron для управления и мониторинга Мобильной установки подготовки нефти (МУПН).

## 📋 Требования

- **Node.js** версии 16 или выше
- **npm** (поставляется с Node.js)
- Для сборки под Windows на Linux потребуется `wine`

## 🔧 Установка

### 1. Установка Node.js

**Windows:**
- Скачайте установщик с https://nodejs.org/
- Запустите установщик и следуйте инструкциям

**Linux (Ubuntu/Debian):**
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

**macOS:**
```bash
brew install node
```

### 2. Установка зависимостей проекта

Откройте терминал в корневой папке проекта и выполните:

```bash
npm install
```

Это установит все необходимые зависимости, включая Electron и electron-builder.

## 🎯 Запуск приложения

### Режим разработки

Для запуска приложения в режиме разработки:

```bash
npm start
```

Приложение откроется в окне Electron. Вы можете нажать `F12` для открытия инструментов разработчика.

### Режим разработки с отладкой

```bash
npm run dev
```

## 📦 Сборка приложения

### Сборка для Windows (создание .exe установщика)

```bash
npm run build
```

или

```bash
npm run dist
```

Готовый установщик появится в папке `dist/`:
- `МУПН Цифровой Двойник Setup 1.0.0.exe` - установщик для Windows

### Сборка для всех платформ

```bash
npm run build:all
```

Это создаст версии для:
- **Windows** (.exe установщик)
- **macOS** (.dmg образ)
- **Linux** (.AppImage и .deb пакеты)

⚠️ **Примечание:** Сборка для macOS возможна только на macOS системе.

## 🎨 Настройка иконок

Для кастомных иконок приложения разместите файлы в папке `build/`:

- `icon.ico` - иконка для Windows (256x256 px)
- `icon.icns` - иконка для macOS
- `icon.png` - иконка для Linux (512x512 px)

### Создание иконок онлайн:
- https://www.icoconverter.com/ - для .ico файлов
- https://cloudconvert.com/png-to-icns - для .icns файлов

## 📁 Структура проекта

```
XTb-25-1/
├── main.js              # Главный процесс Electron
├── preload.js           # Preload скрипт для безопасности
├── package.json         # Конфигурация проекта и зависимости
├── .gitignore          # Исключения для git
├── build/              # Иконки приложения
│   ├── icon.ico
│   ├── icon.icns
│   └── icon.png
├── Главная страница/   # Главная страница приложения
│   └── index.html
├── НГС-1/             # Входной сепаратор
├── ГС-1/              # Газовый сепаратор
├── НГСВ-1/            # Трёхфазный сепаратор
├── П-1/               # Печь-подогреватель
├── Н-1/               # Насосные агрегаты
├── ЕД-1/              # Блок ёмкостей
├── ГФУ/               # Факельная установка
└── dist/              # Скомпилированные установщики (создаётся после сборки)
```

## 🔑 Основные команды

| Команда | Описание |
|---------|----------|
| `npm install` | Установка зависимостей |
| `npm start` | Запуск в режиме разработки |
| `npm run dev` | Запуск с отладкой |
| `npm run build` | Сборка для Windows x64 |
| `npm run build:all` | Сборка для всех платформ |
| `npm run dist` | Полная сборка с упаковкой |

## 🎮 Использование приложения

### Меню приложения

- **Файл** → Главная страница, Обновить, Выход
- **Объекты** → Быстрый доступ ко всем объектам МУПН
- **Вид** → Полноэкранный режим (F11), Инструменты разработчика (F12)
- **Помощь** → О программе

### Горячие клавиши

- `F5` - Обновить страницу
- `F11` - Полноэкранный режим
- `F12` - Открыть/закрыть инструменты разработчика
- `Alt+F4` - Выход из приложения

## 🐛 Отладка

Если приложение не запускается:

1. Убедитесь что Node.js установлен: `node --version`
2. Проверьте установку зависимостей: `npm install`
3. Очистите кэш: `npm cache clean --force`
4. Удалите `node_modules` и переустановите: 
   ```bash
   rm -rf node_modules
   npm install
   ```

## 📝 Настройка сборки

Конфигурация electron-builder находится в `package.json` в секции `"build"`. 

Основные параметры:

- `appId` - уникальный идентификатор приложения
- `productName` - название приложения
- `win.target` - формат установщика для Windows (nsis, portable, zip)
- `nsis.oneClick` - однокликовая установка (false = пользователь выбирает путь)

## 🌍 Кросс-платформенная сборка

### На Linux для Windows:

Установите wine:
```bash
sudo apt-get install wine64
```

Затем выполните сборку:
```bash
npm run build
```

### На Windows для Windows:

Просто выполните:
```bash
npm run build
```

## 📤 Распространение

После сборки в папке `dist/` появятся:

- **Windows:** `МУПН Цифровой Двойник Setup 1.0.0.exe`
- **macOS:** `МУПН Цифровой Двойник-1.0.0.dmg`
- **Linux:** `mupn-digital-twin-1.0.0.AppImage` и `.deb`

Эти файлы можно распространять конечным пользователям.

## 🔐 Безопасность

Приложение использует:
- `contextIsolation: true` - изоляция контекста
- `nodeIntegration: false` - отключение Node.js в renderer процессе
- `enableRemoteModule: false` - отключение удалённого модуля
- Preload скрипт для безопасного взаимодействия между процессами

## 📄 Лицензия

MIT

## 👨‍💻 Разработка

Для добавления новых функций:

1. Основная логика приложения → `main.js`
2. Безопасное API для renderer → `preload.js`
3. Веб-интерфейс → файлы в папках объектов

---

**Создано с помощью Electron** ⚡
