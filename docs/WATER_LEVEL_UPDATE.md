# Обновление регулятора уровня воды

## Дата изменения
13 декабря 2025 г.

## Описание изменений

Вторая вкладка "Регулятор уровня воды в основной камере" была полностью переработана в соответствии с новыми требованиями.

## Новые параметры

### Входные параметры

1. **К (Степень открытия клапана)**
   - Диапазон: 0-1
   - 0 - клапан закрыт
   - 0.5 - оптимальное значение (клапан открыт наполовину)
   - 1 - клапан полностью открыт
   - Тип контрола: ползунок (slider)

2. **Q (Расход жидкости)**
   - Диапазон: 100-1000 м³/сут
   - Тип контрола: ползунок (slider)

### Выходной параметр

**Уровень воды в миллиметрах (мм)**
- Отображается в большом окне
- Цветовая индикация:
  - 🔵 Синий (4fa3ff) - нормальный диапазон (500-2500 мм)
  - 🟠 Оранжевый (ffa940) - предупреждение (320-500 мм или 2500-2880 мм)
  - 🔴 Красный (ff4d4f) - авария (< 320 мм или > 2880 мм)

## Формула расчета

```javascript
уровень = минимальная_высота + рабочий_диапазон × ∛((К - 0.1) / (0.9 × (1 + Q/1000)))
```

где:
- `минимальная_высота = 320` мм
- `рабочий_диапазон = 2560` мм (2880 - 320)
- `К` - степень открытия клапана (0-1)
- `Q` - расход жидкости (м³/сут)

## Аварийные состояния

### Уровень воды > 2880 мм
- **Статус:** АВАРИЯ (критический)
- **Сообщение:** "АВАРИЯ: Уровень воды превышает 2880 мм! Откройте клапан и сбросьте воду."
- **Визуализация:** Красное мигающее окно

### Уровень воды < 320 мм
- **Статус:** АВАРИЯ (критический)
- **Сообщение:** "АВАРИЯ: Уровень воды ниже 320 мм! Закройте клапан."
- **Визуализация:** Красное мигающее окно

## Технические изменения

### HTML (index.html)
- Заменены контролы `waterCv`, `waterDp`, `waterU` на `waterK` и `waterQ`
- Изменён выходной элемент с `waterQ` (%) на `waterLevel` (мм)
- Добавлен класс `large` для большого окна отображения

### CSS (styles.css)
- Добавлены стили для `.value-box.large`
- Добавлены стили для `.large-value`
- Добавлены стили для `<small>` текста в контролах

### JavaScript

#### Model (model.js)
```javascript
// Новые параметры состояния
waterK: 0.5,    // Степень открытия клапана
waterQ: 100,    // Расход жидкости

// Новый метод расчета
calculateWaterLevelMM() {
  const K = this.state.waterK;
  const Q = this.state.waterQ;
  const minHeight = 320;
  const workingRange = 2560;
  const numerator = K - 0.1;
  const denominator = 0.9 * (1 + Q / 1000);
  const ratio = numerator / denominator;
  const cubeRoot = Math.cbrt(ratio);
  return minHeight + workingRange * cubeRoot;
}
```

#### View (view.js)
```javascript
// Новый метод отображения
updateWaterLevelMM(levelMM) {
  const formatted = this.format(levelMM, 0);
  this.elements.waterLevel.textContent = formatted + " мм";
  
  // Цветовая индикация
  if (levelMM > 2880 || levelMM < 320) {
    this.elements.waterLevel.style.color = '#ff4d4f';  // Красный
  } else if (levelMM > 2500 || levelMM < 500) {
    this.elements.waterLevel.style.color = '#ffa940';  // Оранжевый
  } else {
    this.elements.waterLevel.style.color = '#4fa3ff';  // Синий
  }
}
```

#### Controller (controller.js)
```javascript
// Новые обработчики контролов
this.view.bindControl('waterK', (value) => {
  this.model.updateParameter('waterK', value);
  this.view.updateControlValue('waterKVal', value);
  this.updateWaterLevelMM();
  
  // Синхронизация с клапаном
  const valveOpen = value > 0.1;
  if (this.model.state.waterValve !== valveOpen) {
    this.model.updateParameter('waterValve', valveOpen);
    this.updateValves();
  }
});

this.view.bindControl('waterQ', (value) => {
  this.model.updateParameter('waterQ', value);
  this.view.updateControlValue('waterQVal', value);
  this.updateWaterLevelMM();
});

// Новый метод обновления
updateWaterLevelMM() {
  const levelMM = this.model.calculateWaterLevelMM();
  this.view.updateWaterLevelMM(levelMM);
  this.checkAlerts();
}
```

## Примеры работы

### Нормальный режим
- К = 0.5, Q = 100 м³/сут → Уровень ≈ 1200 мм (синий цвет)
- К = 0.6, Q = 500 м³/сут → Уровень ≈ 1400 мм (синий цвет)

### Режим предупреждения
- К = 0.3, Q = 100 м³/сут → Уровень ≈ 450 мм (оранжевый цвет)
- К = 0.9, Q = 200 м³/сут → Уровень ≈ 2600 мм (оранжевый цвет)

### Аварийный режим
- К = 0.1, Q = 100 м³/сут → Уровень < 320 мм (красный цвет + мигающий алерт)
- К = 1.0, Q = 100 м³/сут → Уровень > 2880 мм (красный цвет + мигающий алерт)

## Совместимость

Изменения сохраняют совместимость с остальными модулями:
- ✅ Регулятор давления работает как прежде
- ✅ Регулятор уровня нефти не затронут
- ✅ Визуализация сепаратора функционирует
- ✅ Система рекомендаций активна
- ✅ MVC архитектура сохранена

## Тестирование

Для проверки работы:

1. Откройте `index.html` в браузере
2. Перейдите на вкладку "Клапан регулятора уровня воды в основной камере"
3. Проверьте:
   - Ползунок К (0-1)
   - Ползунок Q (100-1000)
   - Отображение уровня в мм
   - Изменение цвета при разных значениях
   - Появление алертов при К < 0.15 или К > 0.95

## Известные ограничения

- При К < 0.1 формула может давать отрицательные значения из-за кубического корня из отрицательного числа
- Минимальное практическое значение К для избежания аварии: 0.11
- Максимальное практическое значение К: 0.95

## Авторы изменений

Обновление выполнено в соответствии с требованиями пользователя.
